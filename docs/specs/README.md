# Specifications

This directory contains formal interface definitions, schema contracts, API drafts, validation structures, and deterministic protocol documentation.

A specification may exist before deployment, but it must be clearly marked as:

- implemented
- partial
- draft
- deprecated
- experimental

Draft interfaces must not be represented as active production infrastructure unless verified in runtime behavior.
